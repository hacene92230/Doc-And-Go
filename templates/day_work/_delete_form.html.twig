<form method="post" action="{{ path('app_day_work_delete', {'id': day_work.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce jour?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_work.id) }}">
    <button class="btn">Supprimer</button>
</form>
