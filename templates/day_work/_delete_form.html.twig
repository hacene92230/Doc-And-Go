<form method="post" action="{{ path('app_day_work_delete', {'id': day_work.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_work.id) }}">
    <button class="btn">Delete</button>
</form>
