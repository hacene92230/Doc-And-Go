<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path("app_home") }}">DOC-AND-GO</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        {{ render(controller('App\\Controller\\SearchController::new')) }}
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path("app_appointment_index") }}">Prendre un rendez-vous</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path("app_contact_new") }}">Contact</a>
            </li>
        </ul>
        {% if app.user and is_granted('IS_AUTHENTICATED_FULLY') %}
            {% include "shared/menuuser.html.twig" %}
        {% else %}
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_register")}}">Nous rejoindre</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_login") }}">Accéder à mon compte</a>
                </li>
            </ul>
        {% endif %}
        {{ render(controller('App\\Controller\\SpecialityController::index')) }}
    </div>

</nav>
